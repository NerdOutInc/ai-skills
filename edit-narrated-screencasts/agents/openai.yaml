interface:
  display_name: "Edit Narrated Screencasts"
  short_description: "Sync narration and polish screencast edits"
  default_prompt: "Use $edit-narrated-screencasts to sync my narration with an existing screencast and render a preview first."

policy:
  allow_implicit_invocation: true
